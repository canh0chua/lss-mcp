# Path to the directory you want the MCP file tools to access.
# This gets mounted as /workspace inside the container.
# Defaults to the current directory (.) if not set.
WORKSPACE_PATH=/path/to/your/project
